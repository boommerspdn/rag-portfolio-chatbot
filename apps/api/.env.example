PORT=3001
# Prisma / Nest — local Docker Compose uses @db:5432; Neon uses postgres://...@ep-....neon.tech/...?sslmode=require
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/rag_portfolio_chatbot_db
AI_BASE_URL=http://localhost:8000
# Cloud Run production: set AI_BASE_URL to the https://...run.app URL of the ai service (required by env validation).
# Optional: restrict browser CORS to your Next.js origin (e.g. http://localhost:3000). Omit to allow any origin.
# WEB_ORIGIN=http://localhost:3000
# Production (Vercel): set WEB_ORIGIN to https://your-app.vercel.app (or custom domain).
